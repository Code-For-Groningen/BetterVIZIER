
import { localize } from '@codingame/monaco-vscode-api/vscode/vs/nls';

const open = ( localize(1823, 'open'));
const close = ( localize(1824, 'close'));
const find = ( localize(1825, 'find'));
var localizedStrings = {
    open: open,
    close: close,
    find: find
};

export { localizedStrings as default };
